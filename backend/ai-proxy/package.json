{
  "name": "numify-ai-proxy",
  "version": "1.0.0",
  "description": "Backend proxy for Numify Ask AI. Holds the model key; the app never does.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions:aiQuery",
    "logs": "firebase functions:log --only aiQuery"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
