<?xml version="1.0" encoding="utf-8"?>
<!-- Inverted hero card at the top of a tool screen: the one dark block on the
     page, so the headline reading sits apart from the rows beneath it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/cid_900" />
    <corners android:radius="@dimen/_25sdp" />
</shape>
