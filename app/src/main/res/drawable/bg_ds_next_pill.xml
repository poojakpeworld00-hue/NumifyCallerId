<?xml version="1.0" encoding="utf-8"?>
<!-- Onboarding CTA: the same 135deg brand gradient as the language screen's
     Continue, on the 20dp radius the design gives this one. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="315"
        android:endColor="@color/ds_accent_deep"
        android:startColor="@color/ds_accent_bright"
        android:type="linear" />
    <corners android:radius="20dp" />
</shape>
