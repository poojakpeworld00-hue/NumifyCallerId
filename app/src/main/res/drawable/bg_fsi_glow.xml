<?xml version="1.0" encoding="utf-8"?>
<!-- Soft ambient glow behind the incoming-call card (animated: alpha breathe). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <gradient
        android:type="radial"
        android:centerX="0.5"
        android:centerY="0.5"
        android:gradientRadius="150dp"
        android:startColor="@color/fsi_glow"
        android:endColor="@android:color/transparent" />
</shape>
