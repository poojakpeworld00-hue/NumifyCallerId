<?xml version="1.0" encoding="utf-8"?>
<!-- Plan row: a quiet outline normally, the brand wash and a brand border when
     selected. Same activated-state pattern as the language rows. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_activated="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/ds_selected_tint" />
            <stroke android:width="2dp" android:color="@color/ds_accent" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/ds_surface" />
            <stroke android:width="1dp" android:color="@color/ds_rule" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</selector>
