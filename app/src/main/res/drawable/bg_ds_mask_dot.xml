<?xml version="1.0" encoding="utf-8"?>
<!-- One character of a masked name. A real dot per hidden letter, so the length
     of the name is visible while the name is not. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/ds_on_hero" />
</shape>
