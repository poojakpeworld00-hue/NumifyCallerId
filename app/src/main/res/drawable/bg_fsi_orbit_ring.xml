<?xml version="1.0" encoding="utf-8"?>
<!-- Expanding orbit ring behind the incoming-call card (animated: scale + fade). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke android:width="1.5dp" android:color="@color/fsi_orbit_ring" />
</shape>
