<?xml version="1.0" encoding="utf-8"?>
<!-- Benefits list card. Solid white (light) / translucent white (dark) via night colors. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/fsi_benefit_card" />
    <stroke android:width="1dp" android:color="@color/fsi_benefit_border" />
    <corners android:radius="@dimen/_20sdp" />
</shape>
