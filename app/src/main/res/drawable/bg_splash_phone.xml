<?xml version="1.0" encoding="utf-8"?>
<!-- The glowing phone illustration body (glass in light, navy in dark). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/splash_phone_bg" />
    <corners android:radius="@dimen/_30sdp" />
    <stroke
        android:width="1dp"
        android:color="@color/splash_phone_stroke" />
</shape>
